package org.cyk.utility.rest;

public interface RestHelper {
	/*
	static <T> T getProxy(Class<T> klass) {
		String uri = ConfigProvider.getConfig().getOptionalValue(UNIFORM_RESOURCE_IDENTIFIER, String.class).orElseThrow();
		return RestClientBuilder.newBuilder().baseUri(URI.create(uri)).build(ExpenditureService.class);
	}
	*/
}